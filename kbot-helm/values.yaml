# Default values for helm.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.
replicaCount: 1
image:
  repository: "fatcatmeow/kbot"
  tag: "v1.1.3-c23f269"
  arch: "amd64"
  os: "linux"
secret:
  name: "kbot"
  env: "TELE_TOKEN"
  key: "token"
securityContext:
  privileged: true
